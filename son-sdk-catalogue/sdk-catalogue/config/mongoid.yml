---
development:
  sessions:
    default:
      database: son_catalogue
      hosts:
      - 127.0.0.1:27017
      user:
      password:

    ns_db:
      database: ns_catalogue
      hosts:
      - 127.0.0.1:27017
      user:
      password:

    vnf_db:
      database: vnf_catalogue
      hosts:
      - 127.0.0.1:27017
      user:
      password:

test:
  sessions:
    default:
      database: son_catalogue_test
      hosts:
      - 127.0.0.1:27017

production:
  sessions:
    default:
      database: son_catalogue
      hosts:
      - 127.0.0.1:27017
      user: username
      password: password
